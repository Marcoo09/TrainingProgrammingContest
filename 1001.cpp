//http://poj.org/problem?id=1001

#include "math.h"

int main()
{
    int counter = 1;
    while (cin >> s && counter < 7)
    {
        //Read S and store in a long varibale.
    }

    while (cin >> n && counter <= 8)
    {
        //Read n and store in a int varibale.
    }

    //Probably the types passed to frexp functions are wrong
    cout << frexp(s, n) << endl;
}

{
